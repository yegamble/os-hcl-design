/**
 * Local ESLint rules for os-hcl-design.
 * Loaded via eslint-plugin-local-rules — referenced as `local-rules/<rule-name>`.
 */

module.exports = {
  'no-raw-hex-in-className': require('./no-raw-hex-in-className.js'),
};
